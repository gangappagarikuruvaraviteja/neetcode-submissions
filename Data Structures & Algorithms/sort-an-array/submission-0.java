class Solution {
    public int[] sortArray(int[] nums) {
        // int n=nums.length;
        // int []arr=new int[n];
        // int large=nums[0];
        // for(int i=nums.length-1;i>=0;i--){
        //     if(nums[i]>large){
                
        //         arr[i]=nums[i];
        //         large++;

        //     }
        // }
        // return arr;

        Arrays.sort(nums);

        return nums;
    }
}